import { PropsWithChildren } from "react";
import Styles from "./<FTName | capitalize>.module.scss";

type <FTName | capitalize>Props = {};

export const <FTName | capitalize> = ({}: PropsWithChildren<<FTName | capitalize>Props>) => {
    return (
        <div className={Styles.<FTName | capitalize>}></div>
    )
}